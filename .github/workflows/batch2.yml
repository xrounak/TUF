name: Render Batch 2 (q13-q17)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    env:
      START: 13
      END: 17

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Verify Environment
        run: |
          echo "Node:"
          node -v

          echo "NPM:"
          npm -v

          echo "FFmpeg:"
          ffmpeg -version

          echo "FFprobe:"
          ffprobe -version

      - name: Create Render Folder
        run: |
          mkdir -p render

      - name: Render q13-q17
        run: |
          node render-range.js

      - name: Upload Rendered Videos
        uses: actions/upload-artifact@v4
        with:
          name: batch-2-q13-q17
          path: render/
          if-no-files-found: error